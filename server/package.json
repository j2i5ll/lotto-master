{
  "name": "lotto-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply lotto-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply lotto-db --local",
    "db:seed": "npx tsx scripts/seed.ts"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20260301.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.70.0"
  }
}
